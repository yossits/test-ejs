<header>
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark bg-info mt-3">
            <div class="container-fluid">
              <a class="navbar-brand" href="/">yossi</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  
                  <li class="nav-item">
                    <a <% if (active == '/') { %>
                      class="nav-link active"
                      <% } else { %>
                       class="nav-link"
                    <% } %> aria-current="page" href="/">Home</a>
                  </li>

                  <li class="nav-item">
                    <a <% if (active == '/training') { %>
                      class="nav-link active"
                      <% } else { %>
                       class="nav-link"
                    <% } %> href="/training">Training</a>
                  </li>

                  <li class="nav-item">
                    <a <% if (active == '/about') { %>
                      class="nav-link active"
                      <% } else { %>
                       class="nav-link"
                    <% } %> href="/about">About<span class="sr-only"></span></a>
                  </li>

                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                      Courses
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                      <li><a <% if (active == '/courses/angular10') { %>
                        class="dropdown-item active"
                        <% } else { %>
                         class="dropdown-item"
                      <% } %> href="/courses/angular10">Angular 10</a></li>

                      <li><a <% if (active == '/courses/reactjs') { %>
                        class="dropdown-item active"
                        <% } else { %>
                         class="dropdown-item"
                      <% } %> href="/courses/reactjs">React JS</a></li>

                      <li><a <% if (active == '/courses/bootstrap5') { %>
                        class="dropdown-item active"
                        <% } else { %>
                         class="dropdown-item"
                      <% } %> href="/courses/bootstrap5">Bootstrap 5</a></li>

                      <li><a <% if (active == '/courses/java14') { %>
                        class="dropdown-item active"
                        <% } else { %>
                         class="dropdown-item"
                      <% } %> href="/courses/java14">Java 14</a></li>
                    </ul>

                    <li class="nav-item">
                        <a <% if (active == '/login') { %>
                          class="nav-link active"
                          <% } else { %>
                           class="nav-link"
                        <% } %> href="/login">Login</a>
                    </li>

                    <li class="nav-item">
                      <a <% if (active == '/singup') { %>
                        class="nav-link active"
                        <% } else { %>
                         class="nav-link"
                      <% } %> href="/singup">Singup</a>
                    </li>

                    <li class="nav-item">
                      <a <% if (active == '/contact') { %>
                        class="nav-link active"
                        <% } else { %>
                         class="nav-link"
                      <% } %> href="/contact">Contact</a>
                    </li>
                  </li>
                </ul>
              </div>
            </div>
        </nav>
    </div>
</header>